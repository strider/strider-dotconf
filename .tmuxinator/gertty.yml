project_name: Gertty
project_root: ~/
#pre: echo "setup something here!"
pre_window: workon gertty-env
windows:
  - Upstream:
      panes:
        - gertty -c ~/.gertty-openstack.yaml
  - Downstream:
      panes:
        - gertty -c ~/.gertty-internal.yaml
